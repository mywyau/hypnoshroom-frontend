# Insight articles

Article content currently lives in `data/articles.ts` to keep the first deployment dependency-light and fully static.

This directory is reserved for Markdown content. If Nuxt Content is added later, move each article to a Markdown file named after its slug and replace the local data query in the insight pages with a content collection query.
